# 官方版本分阶段合入计划（2026-09-24）

## 基线与决定

- 合并前本地代码：`custom` / `30e125206`，标识 `v1.0.0-rc.16-custom`；当前合并分支的 `VERSION` 已更新为 `v1.0.0-rc.40`。
- 已抓取的官方代码：`upstream/main` / `d04c118c8`；最新发布标签为 `v1.0.0-rc.40`。官方 `VERSION` 文件为空，发布版本由 Git 标签和构建参数注入。
- 生产环境中的两个腾讯视频配置（渠道 ID 219、220，类型 58）已按要求删除；同时删除它们的 7 条能力记录。类型 60 的 4 个渠道配置未改动，历史任务、日志和素材记录未清理。
- 类型 58 保留为已退役编号，不重新分配。已有类型 59–64 的含义和持久化值保持不变；官方新增的 vLLM、SGLang 顺延至 65、66。所有前后端常量、预置路由、渠道表单、能力选择及测试数据必须使用同一映射。
- 正式构建只采用官方单一 `web/src` → `web/dist` 链路；移除 `web/default`、`web/classic` 的构建、嵌入与运行时主题切换行为。

## 合并风险

直接执行一次性 Git 合并产生 444 处冲突，其中约 420 处位于前端。现已在独立分支 `sync/upstream-20260924` 上完成代码整合和第一轮运行修复；工作区尚未提交或部署。下表记录验收范围和剩余风险。

| 阶段 | 内容与保留条件 | 主要风险 | 验收门槛 |
| --- | --- | --- | --- |
| 1. 渠道兼容 | 退役 58 的创建入口；保留 59–64，尤其 60 的请求、轮询、结算与素材链路；将官方 58–63 的新增能力映射到本地 59、63、64、62、65、66。 | **高**：编号错配会把现存渠道按另一供应商处理。 | 对照渠道数据库样本与 API 返回；验证 60 的请求路由、任务查询、费用结算不回归；确认 58 不再可新建。 |
| 2. 后端基础与 GitHub 登录 | 以本地代码为底座，分包移植官方 `relaykit`、请求转换、配置和数据库变更；合入 GitHub 的稳定用户 ID 处理，保留本地浏览器绑定、一次性状态和会话防护。 | **高**：官方把大量根模块 DTO 转移至独立 `relaykit`，直接混用无法编译；认证流程需要安全回归。 | `go build ./...`；`cd relaykit && GOWORK=off go build ./...`；数据库迁移覆盖 SQLite、MySQL、PostgreSQL；GitHub 登录/绑定的失败、过期、重放测试。 |
| 3. 插件能力 | 移植官方插件协议、内置插件、API 和后端管理流程；维持当前插件总开关关闭，保留本地插件计费表达式及渠道绑定规则。 | **中**：插件未投入使用，但协议、计费和通道选择共用核心链路。 | 后端构建与本地启动无报错；检查插件关闭时普通渠道与任务路由可用；按后续要求补跑插件功能回归。 |
| 4. 统一前端 | 使用官方 `web/src`、根 `web/package.json`/锁文件、单个 `web/dist`；移植本地仍在使用的邀请、上传管理、R2 公共上传、任务定价、生成媒体和 60 号渠道配置入口；删除旧主题/旧前端入口。 | **高**：21 个本地独有 TS 模块依赖旧 API 类型或路由；单纯复制会导致类型检查失败和入口缺失。 | `bun install --frozen-lockfile`、`bun run typecheck`、`bun run build`；本地运行检查页面路由/API 调用无报错。按要求跳过视觉展示测试。 |
| 5. 版本与集成 | 将对外版本更新为 `v1.0.0-rc.40` 对应的合并版本，统一 Docker、发布元数据和前端版本；使用独立本地环境启动完整服务。 | **中**：发布配置可能仍引用旧前端产物。 | 构建、启动、健康接口、登录、渠道列表、60 号任务链路和主要 API 的运行检查；Docker 构建后按项目规则清理构建缓存。 |

## 当前验证结果

- 后端 `go build ./...`、独立 `relaykit` 构建、前端 `bun run typecheck` 与 `bun run build` 均通过。
- 隔离 SQLite 实例已完成迁移并启动；`/api/status`、`/api/pricing`、首页和前端路由返回 200，未登录的渠道和视频素材接口返回 401。未接入生产数据库或渠道密钥。
- 运行排查修复了渠道默认地址和视频接口的重复路由注册、后端翻译文件重复键、任务查询无效 SQL、单项请求策略保存后快照不更新、管理员绑定订阅不刷新分组缓存，以及高级自定义渠道的定价端点被元数据覆盖。
- 根模块 `go build ./...`、独立 `relaykit` 构建及其全量测试通过；相关模型、OAuth、60 号渠道、Responses HTTP/WebSocket、渠道配置、异步任务差额结算与零用量退款的定向回归通过。冲突修复还统一了 relaykit DTO 请求入口、上游 HTTP trace、OpenAI reasoning 参数和响应模型日志。
- 按后续要求补齐官方内置插件脚本、插件渠道绑定与 New API 扩展、插件计费的模型映射回退、任务查询状态、即时结算和任务日志。修复了插件表达式结算可能产生负额度的问题；负数或无效结果现在保留已预扣额度。完整 `go test ./...` 已通过，涵盖插件、渠道、relay、controller、service 和计费包。插件前端视觉展示仍未排查。
- GitHub 当前使用稳定数字用户 ID；旧用户名绑定自动迁移仍要求额外验证，尚未移植官方的完整迁移流程。按当前要求跳过 GitHub 测试；真实 60 号渠道调用尚未在隔离环境验证。
- OAuth 浏览器绑定、状态消费与旧 GitHub 用户名不可冒领数字 ID 的定向测试通过。安全检查依据 OWASP ASVS 5.0.0、Authentication、Session Management 与 OAuth 2.0 Cheat Sheets；尚未覆盖真实 GitHub 回调或旧绑定的人工验证迁移。

## 数据库只读核查（2026-09-24）

- 生产 PostgreSQL 仅执行只读查询和 `pg_dump --schema-only`，未对生产库运行新版本迁移或写入。现有 `tasks` 约 168 GB、`logs` 约 4.1 GB；四个类型 60 渠道仍在，类型 58 已无记录，`task_plugins` 与 `prefill_groups` 均为空。
- 将生产结构恢复到独立测试 PostgreSQL 数据库，连续运行两次 `InitDB`/`InitLogDB`。首次迁移的实质结构差异仅为将空的 `task_plugins.source` 从 `text` 改为 `varchar(8388608)`，以及移除空的 `prefill_groups` 表上的旧全局唯一约束；现有部分唯一索引保留。大表 `tasks` 的结构未变化。再次运行无 DDL。
- 隔离 MySQL 数据库连续迁移两次，并运行 SQLite/MySQL/PostgreSQL 的迁移稳定性测试。修复了 MySQL 对等价 decimal 默认值及 MEDIUMTEXT/LONGTEXT 容量误判导致每次启动执行 `ALTER TABLE` 的问题；修复后第二次迁移无 DDL。跨数据库额度与结算测试通过。
- 生产启动时的数据回填预检发现 6 条令牌会把已有旧版自动分组优先级复制到新字段；六条旧值均为有效 JSON 数组，且新字段均为空。用户鉴权版本、外部身份声明等当前无需回填。未读取或输出令牌密钥。
- 部署前于 2026-09-24 16:15 UTC 对生产 PostgreSQL 创建完整自定义格式备份 `backups/new-api-postgres-20260924T161459Z.dump`（约 11 GB）。SHA-256 校验、`pg_restore --list` 以及将整个归档解码至 `/dev/null` 均通过；备份文件留在本机且不纳入 Git。旧生产镜像另存为 `new-api-rollback:20260924-pre-merge`。以上排查仍不能证明生产变更绝对零风险；本记录为部署前状态，尚未对生产数据库执行新版本迁移。
