# 第 5B-1 批阶段进展（2026-09-14）

后续音频与最终审计已完成，第 5B-1 的最终结论见 [验收记录](./ui-batch-5b1-validation-2026-09-14.md)。下文保留本阶段当时状态与证据。

状态：**引擎、同步图片和异步图片核心结算已实现并验证；剩余入口/条件核对尚待完成，未整批验收，未部署生产。** 异步阶段另见 [异步核心进展](./ui-batch-5b1-async-progress-2026-09-14.md) 和 [请求头与 Gemini 进展](./ui-batch-5b1-gemini-progress-2026-09-14.md)；下文同步证据保持原阶段记录。 基准仍为当前 `6cb299e80` 与已核查上游 `7fd063819`，本阶段没有追随更新的上游提交。按用户要求，验证集中在实际扣费、退款、请求数量和旧数据兼容，没有增加纯 UI 或视觉测试。

## 已完成的范围

| 内容 | 影响范围 | 风险及当前证据 |
| --- | --- | --- |
| `fixed()`、显式零价及条件选择 token/按次分支 | 表达式引擎、同步预扣和结算、计费单位与请求条件追踪 | 高：涉及实际扣费。成功图片响应缺少 usage、零价、混合两个分支的实际请求与账目已核对 |
| 显式 `img_cr` 图片缓存价格 | 输入 DTO、OpenAI 图片/Responses/compact 响应明细、token 归一化、消费日志 | 高：需处理图片与缓存重叠。合法明细不重复扣除，缺失/非法明细保留旧处理；实际图片请求及明细手算一致 |
| 旧快照兼容与不支持的入口 | 可序列化快照增加可选字段；Realtime 固定价阻断；保存时阻断尚无 schema 的用量价格 | 中至高：旧 JSON 无新字段仍按原 token 比例结算；未显式提供图片数量不补充隐式数量；未选中的非法/不支持分支不能绕过检查 |
| 验证失败返回 400 | 公共 relay 请求验证失败；过大请求体继续单独返回 413 | 中：实际超量请求发现旧返回 500，已修正并复核为 400；无预扣或上游调用 |

没有修改业务表结构、生产配置、批量旧价格或前端。同步 audio 的固定价结算保护已接入，但本阶段实际 HTTP 证据来自图片端点，不扩张为全部音频入口已经验收。前端仍保留第 5A 批的能力限制，依赖新语义的条件编辑、转换和展示由 5B-2/3 接通。

## 独立本地请求与账目验证

候选服务 `new-api-ui-batch-5b1` 只监听 `127.0.0.1:3332`，使用独立卷 `new-api-ui-batch-5b1-data` 和 `/data/ui-batch-5b1.db`。数据来自既有合成 fixture，再建立合成钱包、受限 relay 令牌、受控 OpenAI 渠道及 8 个测试模型；没有生产数据库或真实上游。所有 New API HTTP 请求通过 newapi 技能提供的 `api.js`，测试凭证只在私有临时文件/子进程环境中使用，未写入审查材料。

同步阶段运行二进制与当时宿主候选的 SHA-256 一致：`ee113d2b1a623dc442293afc7f92292c76c5c3caab83a78584c2653e13b3a64c`。复用缓存镜像、挂载候选 Go 二进制，没有 Docker 镜像构建。

| 实际请求案例 | 最终额度 |
| --- | ---: |
| 固定 USD 0.025，成功响应没有 token usage | 12,500 |
| 显式免费按次请求 | 0 |
| 请求条件选择固定 USD 0.03 分支 | 15,000 |
| 同一混合表达式选择 token 分支 | 350 |
| 合法图片缓存重叠 | 2,110 |
| 非法图片缓存明细，沿用旧归一化 | 2,450 |
| 旧 token 表达式 | 350 |
| 请求 3 张、实际返回 1 张，数量只应用一次 | 12,500 |
| 请求 129 张，400 拒绝且未调用上游 | 0 |
| 上游返回 502，完整退回预扣 | 0 |

共 10 个案例，8 个成功请求（其中 1 个免费），总实际额度 45,260。钱包与受限令牌均从 10,000,000 变为 9,954,740，used_quota 均为 45,260；渠道累计额度与消费日志合计也为 45,260，请求次数为 8。

图片缓存手算使用消费日志解码后的表达式及 `billing_tokens`：输入 1,000，缓存 400，图片 500，其中缓存图片 200；归一化后 `p=300, cr=200, img=300, img_cr=200, img_o=200, c=0, len=1000`。`300*2 + 200*0.5 + 300*5 + 200*0.1 + 200*10 = 4,220`，除以 1,000,000、乘以 quota_per_unit 500,000、乘以组倍率 1，得到实扣 2,110。非法明细时保留旧 `p=100, cr=400, img=500, img_cr=0`，实扣 2,450；此项验证兼容回退，并不把非法上游数据推断为可信重叠。

## 专注源码检查

新增 9 个测试函数，覆盖 fixed 叶节点/计费边界、旧快照、条件追踪隔离、缓存明细及响应传递、未配置 usage-schema 的阻断。复用已有相关计费回归，最终记录为引擎 62、服务/helper/settings 范围 45、OpenAI 范围 3 个顶层测试通过；这些包含已有测试，不宣传为 110 个新测试。Go 候选构建与范围 diff 检查通过，根 `go.mod/go.sum` 未修改。测试采用临时派生 modfile 及当前用户可写 Go cache。

较早一次过宽的图片测试选择触发了既有 DNS/本机监听需求，沙箱阻止后按权限流程重试通过；最终留存的检查已缩小到本阶段相关计费和用量路径。没有运行全仓库测试或视觉验收。

证据：[源码与检查清单](./artifacts/ui-batch-5b1-foundation/source-manifest.json)、[实际请求和账目](./artifacts/ui-batch-5b1-foundation/billing-requests.json)、[隔离服务记录](./artifacts/ui-batch-5b1-foundation/runtime.json)。源码清单只覆盖当前 20 个 Go 实现/测试文件，不代表所有批次或未完成异步路径。

## 待完成与回退边界

后续已接通并验证异步核心、普通请求头冻结及 Gemini 三个图片入口，下文同步证据保留为历史阶段。5B-1 仍需完成任务 clamp 审计及各入口/在途旧任务的最终核对。保留当前大量定制 worker、Seedream、模型数量上限和重试预扣，不能用上游文件整体替换。音频相关实际入口也仍需针对性验证或明确能力限制。

目前无需业务表迁移，但新增 JSON 快照语义不是旧二进制自动可识别的合同。生产切换前需完成异步验证，并明确新表达式/任务快照的回退限制。当前本地回退可停止此专用容器，恢复本批前后端源码或使用上一个隔离候选；不执行全仓库 reset，也不覆盖用户已有改动。测试卷可保留用于后续核对，生产容器保持原样。
