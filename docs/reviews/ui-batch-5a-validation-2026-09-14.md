# 第 5A 批实现与风险记录（2026-09-14）

状态：**本子批源码及必要本地验证已完成，未部署生产。** 固定上游 `7fd06381976cbea126d1f20973548bf60f20e37d`。第 1–4 批已有独立记录；第 5B–8 批待实施。

## 改动、影响与风险

| 改动 | 影响范围 | 风险及保护 |
| --- | --- | --- |
| USD / 站点币种输入、汇率说明 | 模型定价编辑、固定价、token 各价格通道和现有表达式可视编辑 | 中：父级状态、倍率和保存字段仍使用 USD；币种/汇率切换不触发价格改写。非法汇率回退 USD；溢出及非零价格下溢阻止保存，不保存上一次有效值 |
| 精度和本地字段兼容 | 价格格式、倍率推导和保存草稿 | 中：保留常规价格已有的 12 位小数规范；极小非零价格使用有效数字保留，接受换算生成的科学计数法；未编辑价格保留原保存值，本地视频输出价格通道继续使用原字段 |
| 共享滚动区和价格单位 | 当前计费、编辑表单、预览与固定保存区 | 低：当前计费及错误控制移入编辑器滚动区域，保留固定操作区；价格单位和输入说明跟随选定币种 |
| 时间条件说明和当前时段预览 | 模型广场卡片、列表价格/缓存列、详情分组价格、编辑预览 | 低至中：只读解析当前引擎支持的时间条件及线性 token 分段，显示完整条件及当前时间分支；用量/请求条件仍由服务端确定费用，复杂表达式保留原文 |
| 原表达式保留修复 | 已配置时间表达式、普通可视表达式的编辑/保存 | 中：验证发现旧编辑器初始状态可能将表达式改写为零价；初始化时正确选择原文模式，可视表达式也只有实际编辑才生成新表达式，查看和切换币种保留原配置 |

输入币种选择仅保存在浏览器偏好中，不修改站点币种配置。当前时间预览使用共享分钟时钟，页面隐藏时暂停、恢复时刷新；服务端 `Local` 时区无法在浏览器确定时不伪造当前价格。历史日志展示完整快照分支及服务端命中标记，未启用当前时钟，不重新计算历史费用。

没有修改服务端价格 API、乐观版本校验、表达式序列化合约、预扣/结算/退款、异步快照或数据库；没有新增字段、表或迁移。本地已有 `image_count` 语义保持原显示路径，本批不接通 fixed()、img_cr 或插件 u() 等新增计费展示/编辑语义。

## 专注验证

- **3 个文件、16 项通过**：7 项真实编辑器的保存草稿/币种检查，4 项固定日期时间分支与金额摘要检查，以及既有共享价格合约的 5 项检查。覆盖固定价零值、token 倍率、禁用通道、本地视频字段、无修改和真实可视价格修改、汇率切换、溢出/下溢、极小非零价、原表达式/请求规则保留、夜间边界、DST、星期编号、无效时区 UTC 回退、未知服务端 Local 与历史分支不受当前时钟影响。
- 以上是前端保存数据和只读解析的合成回归，**不宣称实际数据库写入、请求结算或端到端计费已验证**。本批后端/数据库没有变化，不扩大 Go 或数据库测试；纯 UI 不做截图、布局或浏览器验收。
- `bun run build:check`（类型及生产构建）通过。28 个涉及源码文件、7 个 locale 与实际构建指纹匹配；独立产物 `/tmp/newapi-ui-batch-5a-final-dist-v2`，349 个文件。
- Scoped lint 没有新增问题；旧编辑器仍有 **4 处既有数组索引 key error**，已与修改前基线核对并留存，不能据此宣称所有 lint 通过。其余旧格式/冗余展开问题已规范化；不为清除这四个既有问题扩大到条件树重构。
- 原版权块及页首位置保留；scoped 格式处理、空白及七语言键检查通过。新增五条当前时段/单位文案通过临时 `add-missing-keys.mjs` 写入七语言并执行 `bun run i18n:sync`，临时脚本已删除。
- 本地候选前端 **127.0.0.1:3330** 使用本批源码，代理到独立后端 **127.0.0.1:3328**（隔离 SQLite/合成配置）；公开状态及合成文档指纹核对通过。沙箱联网失败后执行授权的本地检查，使用公开占位凭证，未访问生产。
- 没有构建 Docker 镜像、使用真实供应商请求、读写生产数据库或部署生产。

证据：[源码及构建产物指纹](./artifacts/ui-batch-5a/build-manifest.json)、[16 项检查](./artifacts/ui-batch-5a/focused-tests.log)、[本地代理核对](./artifacts/ui-batch-5a/runtime.json)、[源码/翻译核对](./artifacts/ui-batch-5a/source-review.json)、[当前 lint](./artifacts/ui-batch-5a/lint.json)、[原编辑器 lint 基线](./artifacts/ui-batch-5a/lint-baseline.json)。

## 回退与下一批

本批没有数据库回退步骤。按本批具体差异撤回币种组件/偏好、时间展示和编辑器改动，保留前序模型广场、版本冲突保护、媒体/视频通道及统一请求失败处理；不能整目录覆盖本地实现。

下一步为 **5B-1：表达式引擎、计费结算及旧快照兼容**，之后接通条件树、请求模拟、旧价转换草稿和日志/模型广场明细。第 5A 的只读预览不证明新计费语义已完成；仍不自动批量转换现有价格，不部署生产。
