# 第 4A 批实现与风险记录（2026-09-14）

状态：**本子批源码及必要本地验证已完成，未部署生产。** 固定上游 `7fd06381976cbea126d1f20973548bf60f20e37d`。第 4B 批付款确认及第 5–8 批继续实施。

| 改动 | 影响范围 | 风险及保护 |
| --- | --- | --- |
| 订单搜索防抖 | 钱包充值记录，自身/管理员订单查询 | 中：复用 500ms debounce，连续输入仅查询最后关键词，搜索重置第一页 |
| 旧请求隔离 | 订单搜索、分页/页大小、刷新、卸载 | 中：过时成功/失败均不覆盖结果或结束新请求的 loading；卸载后不提示旧错误；管理员完成订单后刷新当前搜索 |
| 失败提示统一 | 订单读取、已有管理员完成订单、订阅读取和偏好保存 | 中：业务失败不报告成功，失败完成订单不刷新；使用公共安全错误展示，不输出完整请求错误 |
| 订阅偏好后果说明 | 钱包订阅卡片 | 低：显示真实保存偏好；无有效订阅时，“仅订阅”提示拒绝请求，“订阅优先”提示使用钱包 |

服务端 `service/billing_session.go` 已核对上述两种偏好的行为；本批仅修正展示及前端请求生命周期，没有改动计费算法、订单入账、服务端权限、数据库字段或表结构，没有迁移。卡片保留原有订阅购买、余额限制等本地逻辑和原版权注释位置。

**专注验证：1 个文件、6 项通过。** 使用真实 useBillingHistory、钱包 API 和统一 Axios 流程，受控 adapter 提供合成订单；验证最终搜索/第一页、旧结果及 loading 隔离、过时业务失败、卸载、完成订单刷新当前查询、完成失败不刷新或提示成功。没有真实充值、提供商付款或数据库入账验证的声明，也没有纯 UI/文案测试。

3 个涉及 TS/TSX 文件 scoped lint、保留原版权的格式处理、类型检查及 `bun run build:check` 通过。一次额外的 type-aware lint 因项目没有 tsgolint 可执行程序而未运行，随后按项目既有方式执行 scoped lint；独立 tsgo 类型检查通过，没有增加依赖。说明文案已存在于七语言，没有写入 locale。

候选前端复用 **127.0.0.1:3330** 开发服务并已重新编译本批源文件，代理明确指向独立后端 **127.0.0.1:3328** 及其隔离 SQLite/合成配置。订单行为验证为受控传输测试，不等同于线上或真实付款端到端测试。生产构建另存产物。没有构建 Docker 镜像、读写生产数据库或部署生产。

证据：[源码及构建指纹](./artifacts/ui-batch-4a/build-manifest.json)、[六项测试](./artifacts/ui-batch-4a/focused-tests.log)。无数据回退步骤；只撤回本批订单 hook 和订阅卡片差异，保留前序批次及本地定制。第 4B 和后续批次未据此视为完成。
