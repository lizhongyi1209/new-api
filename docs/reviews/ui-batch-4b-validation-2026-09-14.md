# 第 4B 批实现与风险记录（2026-09-14）

状态：**本子批源码及必要本地验证已完成，未部署生产。** 固定上游 `7fd06381976cbea126d1f20973548bf60f20e37d`。第 4A 的订单搜索/订阅说明已独立完成，第 5–8 批待实施。

## 改动、影响与风险

| 改动 | 影响范围 | 风险及保护 |
| --- | --- | --- |
| Waffo 先报价后确认 | 钱包、既有 Waffo amount/pay 接口调用 | 中：选择方式只报价，取消不创建订单；确认后提交既有服务端支付方式索引，包含显式索引 0 |
| 金额/方式确认快照 | 钱包付款选择及统一确认框 | 中：只有最新、有限且正数的成功报价可打开确认；保存本次充值数量、付款金额和方式；金额变更/关闭/卸载使旧选择失效 |
| 提交并发保护 | 钱包确认及处理状态 | 中：同步锁阻止同一轮重复点击；处理期间禁止取消/切换；Waffo 处理状态进入已有确认框；失败保留确认，成功关闭并刷新用户资料 |
| 保留原有金额保护 | Epay/Stripe/Pancake/Waffo 报价 hooks、充值卡片 | 中：保留金额计算代次及余额上限错误提示；增加 Waffo 专用报价分支，不采用会删除本地保护的上游整文件实现 |
| 付款失败提示归属 | 钱包付款/兑换、订阅购买/商品创建、付款设置已有确认/保存 | 中：补齐第 3B 留到付款批次的普通失败处理；使用统一安全错误展示，业务失败不进入确认事项的成功回调；商户凭证验证路径仍随第 6 批核查 |

复用现有 PaymentConfirmDialog 的付款明细；其确认/取消布局使用共享 ConfirmDialog，而不是再拼装通用确认行为。沿用 Epay、Stripe、Pancake 的原请求字段和跳转方式；Waffo 仅接通已有报价端点，不修改服务端付款、回调、订单入账、计费或数据库逻辑。没有新增表、字段、迁移、真实付款、生产域名或支付配置更改。

## 专注验证

- **2 个文件、16 项通过**。报价 hook 验证原有上限理由/并发保护、Waffo 专用报价和无效金额；真实 Wallet、付款 hooks、API 与确认弹窗配合受控 Axios adapter，验证只报价/取消、索引 0 和 1、重复点击、付款失败及成功后的状态、旧报价/金额变更隔离、现有 Epay/Pancake 分派。
- 测试替换非付款卡片和配置数据来源，使用合成金额、订单响应及测试域名；成功跳转通过 window.open spy 验证，不访问支付提供商。这是前端请求/状态回归，不是实际支付、回调或数据库入账端到端验证。没有布局、截图或纯 UI 测试。
- 14 个涉及 TS/TSX 文件 scoped lint 无 error；保留原有 Safari 检测的 includes 建议 warning。全部原版权块及原有页首位置保留，scoped 格式处理、类型检查、`bun run build:check` 和 diff 空白检查通过。移植使用现有七语言文案，没有新增 locale。
- 当前候选前端 **127.0.0.1:3330** 已重新编译本批源码，明确代理到独立后端 **127.0.0.1:3328**（隔离 SQLite/合成配置）。安全 API 脚本经前端代理的公开状态核对通过；沙箱本地联网失败后使用授权的本地检查，没有使用真实凭证。3330 是开发服务，生产构建另存产物。
- 服务端和数据库本批没有改动，未扩大 Go/数据库测试；没有构建 Docker 镜像、读写生产数据库或部署生产。

证据：[最终源码及产物指纹](./artifacts/ui-batch-4b/build-manifest.json)、[16 项检查](./artifacts/ui-batch-4b/focused-tests.log)、[本地代理核对](./artifacts/ui-batch-4b/runtime.json)。第 4A 的六项结果仍是其独立批次记录，不宣称本轮重复运行。

## 回退与后续边界

没有数据库回退步骤。按本批具体差异撤回 Waffo 报价/分派、钱包确认快照/锁、确认框和相关普通失败提示，保留第 4A 搜索、前序公共请求处理和本地余额上限/并发保护；不要用上游文件整目录覆盖。商户凭证验证、凭证刷新及一次性证明属于第 6 批；计费表达式属于第 5 批。本批完成不代表第 5–8 批或整体目标完成。
