# 第 3C 批实现与风险记录（2026-09-14）

状态：**本子批源码和必要本地验证已完成，未部署生产。** 对照固定上游 `7fd06381976cbea126d1f20973548bf60f20e37d`，第 4–8 批仍待实施。

## 改动、影响与风险

| 改动 | 影响范围 | 风险及保护 |
| --- | --- | --- |
| 取消及旧请求隔离 | 在线测试的 SSE、普通请求及消息缓冲 | 中：停止、替换、卸载后忽略旧事件及回调；停止保留尚未刷出的文字；完成时保留最后一段内容 |
| 流连接生命周期 | 在线测试请求控制器 | 中：异步准备期间可取消；连接构造失败结束生成状态；旧连接不能清理新连接状态 |
| 未保存编辑提醒 | 在线测试消息编辑 | 中：取消/退出编辑先确认，停留保留草稿；有改动时注册刷新提醒，恢复原文或卸载时移除 |
| 流式文字渐入 | 回复及推理的共享 Markdown 展示组件、专用 CSS | 低：代码/数学等结构保留原呈现，正文与推理使用独立解析器，尊重减少动画偏好 |
| 概览向导入口 | 分析概览及其页头 | 低：完成后的向导收起为页头按钮；复用既有完成状态和页布局 |

没有新增数据库字段、业务表、迁移或服务端写入接口。付款、余额及预扣/结算算法没有改动。请求参数和既有凭证读取方式保留；请求时刷新凭证归第 6 批。本批的编辑提醒覆盖退出编辑与刷新，不代表所有站内路由切换均受保护。

移植时修正两处上游边界：停止后的延迟 React 状态更新仍可保存最后一段缓冲文字，但不能进入下一次回复；SSE 构造抛错时退出生成状态。前序批次和本地定制代码保持在各自差异中，未整目录覆盖。

## 专注验证

- **3 个测试文件、14 项通过**。真实请求控制器和聊天 hooks 配合受控 SSE/Axios adapter，验证取消、替换、卸载、异步准备、构造失败、最后一段文字及普通请求旧响应隔离；真实消息编辑组件验证草稿确认和刷新提醒。
- 14 个涉及 TS/TSX 文件 scoped lint 与保留全部原版权注释的格式处理通过；类型检查与 `bun run build:check` 通过。新增入口使用的文案已存在于七语言，没有新增 locale 键。
- 当前候选前端开发服务运行于 **127.0.0.1:3330**，明确代理到已有隔离后端 **127.0.0.1:3328**（`new-api-ui-batch-3b`），后端使用独立 SQLite/合成配置。安全 API 脚本经该前端代理读取公开状态，success 与 data 检查通过。生产构建另行保留为构建产物；3330 是开发服务，不是生产部署。
- 请求行为测试使用受控传输，不宣称完成真实提供商调用、付款或计费结算端到端验证。本批没有后端/数据库改动，未扩大数据库测试；按用户要求不做向导排版、渐入动画、截图或纯 UI 浏览器验收。
- 未构建 Docker 镜像，未读写生产数据库、切换域名或部署生产。

证据：[源文件与构建产物指纹](./artifacts/ui-batch-3c/build-manifest.json)、[针对性测试结果](./artifacts/ui-batch-3c/focused-tests.log)、[本地代理检查](./artifacts/ui-batch-3c/runtime.json)。

## 回退

本批无需数据回退。按本批文件的具体差异撤回请求生命周期、编辑提醒、概览入口和共享流式展示，保留前序批次及原有定制；新增 fade 模块与其 CSS 应一并撤回。独立前端服务可单独停止，不影响生产。当前不将后续批次或整体目标标记完成。
