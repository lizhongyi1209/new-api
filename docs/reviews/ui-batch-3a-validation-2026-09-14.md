# 第 3A 批实现与风险记录（2026-09-14）

本批代码及必要本地验证已完成，未部署生产。对照固定上游 `7fd06381976cbea126d1f20973548bf60f20e37d` 的 `web/src/`，移植到当前 `web/default/src/`，保留本地定制。第 3B/3C 和第 4–8 批尚未完成。

用户最新要求：测试专注于线上用户功能、数据库及实际业务风险；纯 UI 正确性无需测试。因此本批不再以全站截图、七语言逐页点击或严格视觉验证作为交付条件。保留必要类型、lint、格式、翻译完整性和构建检查；对查看范围、数据隔离和详情记录身份进行针对性回归。已执行的历史检查如实保留，不继续扩展。

## 功能和影响范围

| 功能 | 影响范围 | 实际风险 |
| --- | --- | --- |
| 公共表格列显隐立即刷新 | 各 DataTableRow 消费者，保留选择、渲染器及样式契约 | 低：渲染刷新，不新增数据写入 |
| 表格密度和宽度 | 公共 Table、列宽、徽标与卡片对齐；原容器默认行为保留，固定表头复用无容器 Table | 低：显示与排版 |
| 手机普通日志卡片/分页 | 可点击模型、映射、时间及元数据详情；缓存和时间摘要，前后页边界；复用既有费用单元格及用户资料入口 | 低至中：新详情必须跟随正确记录，不能泄露已隐藏元数据 |
| 日志查看范围隔离 | self/admin/root 分开查询缓存、占位数据及本地列偏好；切换范围不暂显此前范围记录 | 中：关系到用户可见数据；服务端鉴权和日志查询实现不变 |
| 活动时间和密钥手机卡片 | 密钥创建/最近使用、用户创建/最近登录合并；手机模型/IP 点击详情、继承用户分组说明 | 低至中：显示与详情状态。原密钥读取、复制和操作实现保留，手机卡片按密钥 ID 维持身份 |
| 抽屉与选择器 | 抽屉内 Select/Combobox 浮层容器，手机侧栏层级和工具提示；分组列表固定行高、顶端排列；MultiSelect 粘贴提交全部分隔项 | 低至中：影响多个表单的交互；粘贴更新选择草稿，不自动保存配置 |
| 表单标记与搜索 | 模型/厂商名称使用统一必填标记；密钥及部署搜索复用 500ms 防抖，模型广场本地搜索 200ms 防抖 | 低：沿用原校验和查询 API，没有新增数据库写入 |
| 公共页页头 | 复用第 1A 批管理员版本入口，导航及站点名适应窄屏 | 低：已有只读入口的新增消费者，保留本地链接键和品牌 |

本批没有后端业务修改、表结构或迁移，没有支付、计费计算、渠道编号、认证流程、媒体域名或生产配置变更。总体为**低至中风险**，重点是前端数据隔离和用户操作；纯展示部分风险低。

## 核查结论与批次边界

- 核查公共组件及 hooks，忽略版权注释和格式差异并核对路径迁移。216 个对应文件归一化相同；剩余 22 个差异文件及一个上游独有文件逐类处理，清单见产物目录。这只是源码核查结果，不代表全站浏览器验收。
- 公共 DataTablePage/Toolbar 剩余差异是声明位置、调用顺序及注释；Combobox 保留当前 options 兼容重载和手机 anchor。GroupBadge 保留当前数值倍率样式，增加继承说明；MaskedValueDisplay 保留既有按钮组合，达到相同展示行为，不为上游单纯提取 helper 重写密钥请求。
- 页头保留当前链接标识和认证展示分支，补齐响应式及管理员版本入口。备案页脚、推荐计划、上传管理、主题切换、useIsSuperAdmin 等本地能力继续保留。
- AI 回复流式组件及 response-fade 属 3C；统一通知失败处理属 3B；Markdown 相对文档地址能力随公共/插件文档按 3B/第 7 批处理；移动聊天链接预加载和退出流程属第 6 批。本批不提前改变凭证请求时机。
- 模型/分组选择器主体与上游相同，仅分组滚动区补齐行高/排列。预填分组核心选择布局已存在，其错误处理归 3B。部署名称检查和费用预估已存在，补搜索防抖；失败状态归 3B。
- 任务手机卡片保留当前 task_details 和任务审计入口；插件、产物等新字段需要第 7 批后端支持，不能用不存在的数据替换当前详情。普通日志费用复用当前计费字段和单元格，图片缓存/混合计费明细归第 5/7 批。
- 合并时间列会改变列偏好粒度：密钥旧 created_time/accessed_time 不再分别控制两个时间列，之后统一通过“时间”列设置。偏好是浏览器显示配置，不涉及数据库或历史时间数据。

## 必要验证与证据

- 最新代码 `bun run typecheck`、50 个涉及 TS/TSX 文件 scoped lint/保留原版权头的格式处理、前端构建通过；178 个受影响字面量翻译键在七语言完整。新增 Vendor Name 文案通过规定脚本写入七语言并执行 i18n:sync，临时脚本已删除。
- 最终针对性回归 **2 个文件、13 项通过**：self/admin/root 分类，root/self 及 root/admin 缓存和列偏好隔离，敏感元数据详情关闭，以及刷新列表不能将详情替换成其他记录。两个文件还保留此前已完成的模型详情、缓存摘要和用户资料入口回归，没有扩展视觉测试。
- 用户收紧范围之前执行过前端全套 **80 个文件、528 项通过**。这是最终卡片 ID 修正之前的历史快照；最终修正由上述针对性回归和最新编译/构建覆盖，不宣称最终版本再次全套运行。
- 前两阶段已执行的三组表格和十组密钥浏览器证据见 [历史进展](./ui-batch-3a-progress-2026-09-14.md)。日志七语言浏览器脚本未完成执行，没有生成通过报告；按用户指示停止扩展，不把准备好的脚本算作通过证据。
- 隔离 preview 使用 `127.0.0.1:3327`，代理到测试容器 `new-api-ui-batch-3a` / `127.0.0.1:3326`，独立卷 `new-api-ui-batch-3a-data`。使用九条合成密钥和 45 条合成日志，未复制生产数据库或凭证；新增长模型/映射/缓存样本只修改隔离测试数据。
- 历史连续浏览器刷新触发现有会话刷新限流，记录保留在进展报告；没有调整生产或测试限流策略，没有将重启后的 UI 检查视为认证错误处理完成。

[最新源文件指纹、构建与回归日志](./artifacts/ui-batch-3a-final/build-manifest.json)包含当前 50 个 TS/TSX 和七个语言包。没有 Docker 镜像构建、生产部署或生产写入。preview 和第 2 批候选后端二进制是隔离验证组合，不是正式镜像打包结果。

## 回退与后续

无需数据迁移。回退只恢复本批具体源文件差异，保留第 1、2 批和用户原有修改；不得 reset 整个工作区。浏览器列偏好可重新设置，不需要回滚数据库。测试服务可单独停止。

下一批为 **3B：业务失败状态、提示去重、公共文档缓存及非登录旧地址兼容**。重点保护失败不显示成功、旧响应不覆盖新数据、缓存不存用户特定内容；涉及登录/会话/凭证机制的改动留给第 6 批。生产部署另按授权安排。
