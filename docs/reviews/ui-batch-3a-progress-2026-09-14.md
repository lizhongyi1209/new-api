# 第 3A 批进展（2026-09-14）

本文保留第一、二阶段当时的记录。第 3A 批后续代码及必要验证已完成，最新状态、用户调整后的验证标准和影响范围以 [最终记录](./ui-batch-3a-validation-2026-09-14.md) 为准；历史“待完成”描述不代表当前整批状态。

第 3A 批已开始，未整批验收、未部署生产。当前完成公共表格列显隐刷新、手机普通日志紧凑分页、Vaul 抽屉内浮层容器接通，以及活动时间、API 密钥手机卡片/限制详情和分组继承提示。其他日志卡片、表格与选择器/表单布局增量仍待继续核查和移植；第 3B/3C 与第 4–8 批继续保留。

## 当前改动及影响

| 改动 | 行为和影响 |
| --- | --- |
| 列显隐 | 行对象保持引用时，记录当次可见列编号参与缓存比较；隐藏/恢复列会立即刷新行内容。保留已有行选择、列渲染器及样式刷新契约 |
| 紧凑分页 | 公共分页新增可选 compact，DataTablePage 新增 compactPagination；仅手机普通日志启用，保留已有分页大小、查询和权限。桌面及其他消费者保持原分页 |
| 抽屉容器 | 新共享 PortalContainerContext，Vaul DrawerContent 提供自身 ref；桌面 Select 和通用 Combobox 在抽屉内将选项挂到该容器，避免落在受禁用的外部层。保留本地 Combobox options 重载、移动 anchor 容器及 Select 手机内联兼容 |

直接对照仓库内可用的固定上游提交 `7fd063819`，没有依赖日期较旧的 /tmp 解压目录作为最新证据。现有 Sheet 与固定上游实现相同；列显隐问题在行缓存层，ViewOptions 本身也与上游相同，未重复重写。公共组件继续复用现有 Drawer、Select、Combobox、Tabs/DropdownMenu 和 DataTablePage，没有新增第三方包。

风险 **中**：公共行渲染和抽屉选择器可能影响多个页面，需要继续业务回归；本阶段没有后端业务逻辑、表结构、计费、登录、媒体输出或生产配置变化。

## 当前验证

- `bun run typecheck` 通过；11 个涉及 TS/TSX 文件 scoped oxlint 和保留原版权头的 oxfmt 通过；`git diff --check` 通过。Combobox 原文件的引号/分号随项目格式规范统一，业务兼容入口保留。
- 相关测试 **16 个文件、52 项通过**：包括第 2 批 43 项渠道回归，以及抽屉选项点击/焦点与既有 Combobox、列隐藏恢复、紧凑分页前后边界/空数据回归。不能将 52 项称为全站测试。
- 16 个受影响的字面量翻译键在七语言均存在；本阶段复用已有文案，没有编辑语言包。
- 第 3A 批独立前端构建成功；独立 preview `127.0.0.1:3327` 同源代理至独立后端 `new-api-ui-batch-3a` / `127.0.0.1:3326`，独立卷 `new-api-ui-batch-3a-data`、SQLite `/data/ui-batch-3a.db`。后端复用第 2 批候选二进制，未因纯前端修改重编业务逻辑。
- 测试数据库由此前合成数据复制，并新增 45 条合成普通日志以验证三页导航；未复制生产数据库或凭证。仅在停服时替换隔离数据库。
- [真实浏览器报告](./artifacts/ui-batch-3a-table/report.json) **3 组检查通过**：渠道表格列隐藏/恢复无需刷新；320px 日志三页前进/返回及禁用边界，无文档横向溢出；切换桌面恢复完整分页。没有 pageerror。
- 抽屉选项的 4 项回归在本地组件测试执行；其余真实页面中的模型/分组抽屉仍需随本批剩余工作继续浏览器验证，不将普通表格浏览器结果作为全站抽屉完成证明。

截图、脚本、构建日志及源文件指纹见 [本阶段产物](./artifacts/ui-batch-3a-table/build-manifest.json)。没有 Docker 镜像构建，没有生产部署。当前 preview 和绑定二进制是隔离验证环境，不是正式镜像打包结果。

## 后续与回退

继续逐项完成其余日志卡片、选择器、表单标记和表格布局差异，再做整批本地验收。错误提示/文档缓存/旧地址归 3B，概览及在线测试归 3C，不以当前公共组件检查代替后续范围。

无需数据库迁移。回退仅恢复本阶段具体源文件差异，保留第 1、2 批和用户原有修改；不得 reset 整个工作区。测试容器/preview 可单独停止，生产升级另按授权安排。

## 第二阶段：活动时间和密钥手机卡片

| 改动 | 影响范围 | 风险 |
| --- | --- | --- |
| 共享活动时间 | 密钥桌面合并创建/最近使用时间，用户桌面合并创建/最近登录时间；手机卡片复用时间组件。零/负时间显示横线，保留密钥相对时间、用户绝对时间及长期未用提示 | 低至中：列布局和列偏好需要继续核查，不改变后端时间字段 |
| 手机密钥卡片 | 独立卡片展示状态、原有密钥操作、额度、模型/IP 限制及三项时间；限制详情由悬停改为可点击弹层，Escape 关闭并返回焦点 | 中：密钥页及公共浮层；原 ApiKeyCell 显示/复制函数与 HEAD 完全一致 |
| 用户分组继承提示 | 空/纯空格分组显示“用户分组/继承”，不误显示显式倍率；长分组可聚焦查看，自动跨组动画保留。GroupBadge 新增可选说明和容器样式，旧数值倍率行为保留 | 低至中：密钥分组与共享 GroupBadge/TruncatedCell 消费者 |

没有后端业务修改、数据库迁移、计费计算、认证流程或渠道类型编号变更。没有安装依赖或构建 Docker 镜像。第二阶段保留当前密钥读取授权、自动分组视觉和额度组件。

验证证据：

- 当前类型检查、23 个 TS/TSX 文件的 scoped lint/格式检查以及候选前端构建通过；**24 个文件、87 项相关测试通过**，覆盖渠道、密钥、公共表格、抽屉选项和活动时间。此数字不是全站测试。
- 62 个受影响字面量翻译键在七语言均非空；复用已有 `{{count}} models`，没有新增或编辑语言包。
- [真实浏览器密钥报告](./artifacts/ui-batch-3a-keys/report.json) **10 组通过**：桌面密钥/用户时间列，七语言 320px 深色卡片及限制详情/焦点返回，自动及长名称分组。无 pageerror；限制详情检查中密钥读取请求为 **0**。
- 最新候选前端再次执行 [公共表格浏览器检查](./artifacts/ui-batch-3a-keys/public-table-report.json)，原三组列显隐/手机分页/桌面恢复检查通过。
- 独立测试库增加三条合成密钥用于空分组、自动分组及长分组，合计九条测试密钥；数据库只在隔离实例停止期间替换。报告和截图不保存可用密钥或数据库。
- 初次七语言检查通过；随后连续执行补充检查触发现有 `/api/user/auth/refresh` 安全限流（429），导致登录跳转。仅重启隔离实例后，保留限流配置单独复查，深色主题及分组检查通过。未修改生产限流，也未将本次检查解释为会话刷新错误处理已完成；相关后续处理仍按 3B/第 6 批边界核查。
- 生产容器只读检查为 `running healthy`。没有部署，测试 preview 与独立后端二进制仍不能作为正式镜像打包结果。

第二阶段截图、执行脚本、构建日志与 23 个源文件指纹见 [产物清单](./artifacts/ui-batch-3a-keys/build-manifest.json)。第一阶段目录保存的是当时版本，第二阶段证据以本节目录为准。其余 3A 及后续批次仍待实施。
