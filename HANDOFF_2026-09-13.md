# 交接进度（2026-09-13 UTC）

## 恢复执行后的补充验证（最新，覆盖下面的旧进度）

### 已完成生产发布（2026-09-13 14:28 UTC）

- 当前线上版本：`6cb299e801e902dacffffbd7c42006a736deddb6`（`fix: harden image billing and authorization policies`）。
- 本地发布 tag：`production-20260913-1428-6cb299e801e9`。Git 分支 `custom` 比 `origin/custom` 超前 2 个提交，**没有 push 任何分支或 tag**。
- 正式镜像构建成功，revision 与提交完全一致；保留镜像 `new-api-release:6cb299e801e9-20260913`。构建后按项目规则清理了 7.457 GB Docker build cache。
- 新全库备份：`backups/new-api-postgres-20260913T1406Z.dump`，大小 **21,312,700,491 bytes**，权限 `0600`。
- 备份 SHA256：`338e3b6252c1acd47329be28bb458c83c7b1b18b71134aead840e242063c3b34`；PostgreSQL 容器内原件与主机完整副本完全一致；`pg_restore --file=/dev/null` 全量读取/解压退出成功。没有把全量备份恢复到生产；隔离恢复验证使用的是核心数据副本。
- 生产容器已健康，重启计数为 0；公共 `/api/status` 返回当前完整 SHA。图片生成/编辑、Kling Omni/Motion Control、Grok 三个视频入口的无有效凭据探针全部正确返回 401，没有落入 HTML，也没有调用付费上游。
- 上线后的表结构与隔离升级结果一致（忽略 pg_dump 版本和随机保护标记）；没有非预期 DDL。主数据库和 Redis 的启动时间仍为 2026-07-07，未被重启。
- 上线后核心表行数：users 312、tokens 1097、channels 101、options 68、casbin_rule 5。未发现 startup fatal、panic 或 migration failure。
- 发布后短时观察完成：连续 4 次容器健康探针退出码均为 0，重启计数仍为 0；再次访问公共状态接口仍健康且版本正确。剩余磁盘约 326 GB。
- 本轮使用 `newapi` 技能保护凭据，状态/无凭据路由探针使用技能提供的 API 脚本，没有读取或输出真实密钥。
- 下方关于“尚未提交/备份仍进行/尚未部署”的内容是旧交接快照，不再代表当前状态。代码与发布工具已经提交；本节上线实测记录属于发布后的文档更新，尚未单独提交。

### 若需回退

只回退 API 镜像，不重置 Git、不覆盖数据库、不重启数据库和 Redis：

```bash
docker tag new-api-rollback:726ea223-20260913 new-api-new-api:latest
docker compose up -d --no-build --no-deps new-api
```

回退后确认 health 与 `/api/status` 的 SHA 为 `726ea223b84639876b215a506dd7fd0020a5542e`。本次迁移是兼容的新增结构，保留新增结构即可；**不要直接用发布前备份覆盖仍在写入的生产库，以免丢失备份后新产生的数据**。若未来确需数据库恢复，应先再次备份并制定增量数据保全方案。

### 发布前验证详情

- MySQL、SQLite 完整候选应用首次启动和重启均健康。
- 已在隔离 PostgreSQL 中恢复生产原始表结构，候选升级和再次重启均正常。
- **需要更正结构风险表述**：这次三项合并没有 schema 变更，但从生产回退版本 `726ea223` 升级到待发布基础版本会新增 `users.access_token_created_at` 和 `audit_logs` 表/序列/索引；`subscription_plans.price_amount` 的默认值表示从 `0.000000` 规范化为等价的 `0`。未发现删表、删列或类型缩窄，第二次启动没有重复结构变化。
- 生产权限表 `casbin_rule` 共 5 行，没有 malformed 或 legacy scoped 规则。
- 已恢复生产核心数据到禁止外网访问的独立 Docker 网络 `new-api-release-data-isolated`，不发布测试 API 端口。
- 核心数据升级前后指纹相同：账户、余额、1097 个 token、101 个渠道、预填组、套餐与订阅。68 项配置仅已有后台任务的 `GenerateImageDataCleanupWatermark` 正常推进，其余配置完全一致。该图片数据清理逻辑与生产回退版本一致，并非本次新增。
- 核心数据测试容器 `new-api-release-data-check` 已停止，避免继续运行包含生产数据副本的测试进程。
- 关键 Go 回归测试再次通过，`git diff --check` 通过。
- 已保留不可变回退镜像 `new-api-rollback:726ea223-20260913`，镜像 ID `sha256:5153aaf39e1ac77f4725d6da32b0f5d28368a9b826455753c9eacaef9034b148`。
- 新增 `.gitignore` 的 `/backups/` 规则；备份不得加入 Git。
- 部署工具新增显式 `LOCAL_RELEASE=1` 模式：仍要求干净 `custom` 工作区以及远程/生产 tag 祖先检查，但不会 push Git refs，满足用户此前“不推送 Git”的要求。
- 全库备份与副本校验已完成，然后才执行生产 API 切换；发布过程复用现有部署工具的 check/build，在构建完毕后按同一流程部署已验证镜像，额外使用 `--no-deps` 保证不操作数据库和 Redis，并配置了失败自动回退。

## 当前目标

用户已授权：将已评估的三项官方上游功能合并到本地未推送提交 `f0bdcc4f0` 之上，完成本地与数据库风险验证后，创建一个新版本并部署到生产。

本次**只合并**以下三项：

1. 权限策略的 legacy scope 安全处理。
2. 图像请求数量、预扣费、重试与结算的安全处理。
3. 已存在多密钥渠道的策略编辑修复。

明确**没有合并** Passkey 多 RP ID 支持、上游数据库迁移相关提交，或其他未评估上游功能。

## Git 与生产状态

- 工作目录：`/home/ubuntu/new-api`
- 当前分支：`custom`
- `HEAD`：`f0bdcc4f0 fix(db): harden migrations after upstream sync`
- `origin/custom`：`0150c42f7`；本地分支显示 `ahead 1`（即 `f0bdcc4f0` 尚未推送）。
- 本轮合并改动**尚未提交**，也**尚未推送 Git 远程**，更**尚未部署生产**。
- 生产容器 `new-api`、生产 PostgreSQL `postgres` 与 Redis `redis` 当前均在运行；本轮未改动生产服务或生产数据库。
- 生产仍是此前已回退并验证正常的版本。部署前必须再次确认生产状态和创建新的数据库备份。

## 已实现的改动

### 1. 权限策略安全

- `service/authz/adapter.go`：保留 legacy scoped policy 行，但加载时将无法正确执行的 scope（如 `own` / 非 `all`）转成内存中的显式拒绝，避免跳过规则后回退到管理员基线权限。
- `service/authz/seed.go`：重置内置角色基线时不删除 legacy scoped 行，保留给管理员审查。
- `service/authz/authz_test.go`：覆盖主/从节点、scoped allow/deny、重复初始化与持久化行不丢失。

业务影响：历史 scope 规则不再可能扩大权限；原先依赖这些未受支持 scope 的账号会被保守拒绝，管理员需要审核后改成当前模型支持的全局授权。

### 2. 图像计费安全

- 统一验证顶层 `n` 和 Ali `parameters.n`；Ali 的显式 `0`、超过 `128`、负数、浮点数或 uint 溢出在预扣费前以 400 拒绝。
- 按最终将要发往上游的请求体（包括 channel override、pass-through、重试）计算数量；Ali 强制发送与预扣数量一致的 `parameters.n`。
- 已知图片数量的补充预扣改为钱包原子保留；额度不足时在提交上游前返回 403，不再形成钱包欠费。
- 修复同一请求多次补充保留时退款只退最后一次额度的问题：退款会退回全部已保留额度。
- tiered expression 增加受验证的 `image_count` 输入、快照与结算追踪；结算数量会写入计费日志。
- 图像模型识别补全了当前 Ali/wan/qwen/z-image 与 `gpt-image-*` 模型，避免图像端点被错误分类。

主要文件：`dto/openai_image.go`、`relay/image_handler.go`、`service/image_billing.go`、`service/billing_session.go`、`service/funding_source.go`、`relay/helper/price.go`、`pkg/billingexpr/*`、Ali/OpenAI 图像适配器。

业务影响：这是安全修复，但会改变边界行为：无效 provider 数量会报 400；实际无足够可用额度的图片请求会报 403；此前少扣或退款不完整的情况会按正确数量结算。

### 3. 多密钥渠道策略编辑

- 已存在的多密钥渠道现在显示策略选择器。
- 具有 `channel.sensitive_write` 权限的管理员更新时会发送 `multi_key_mode`。
- 密钥输入框留空时仍不会覆盖现有密钥；无敏感写权限时前端不会发送该策略或密钥字段。

主要文件：

- `web/default/src/features/channels/components/drawers/channel-mutate-drawer.tsx`
- `web/default/src/features/channels/hooks/use-channel-mutate-form.ts`
- `web/default/src/features/channels/hooks/__tests__/use-channel-mutate-form.test.tsx`

### 4. 备份保护与测试工具安全

- 发现 `backups/` 当前约 26 GB；其中包含生产 PostgreSQL 备份和历史 partial 文件，必须保留，不得清理、移动或加入 Git。
- 已把 `backups` 加入 `.dockerignore`，避免 Docker 构建把数据库备份传入构建上下文。
- 已修改 `scripts/verify-upstream-test.sh` 的候选摘要计算，跳过 `backups/*`，避免为测试候选镜像哈希 26 GB 备份文件。

## 已通过的验证

### 后端与计费

- `/usr/local/go/bin/go test ./...`：通过。
- 图像数量/预扣费/重试集成测试：通过，覆盖 Ali 嵌套数量、空参数回退、channel override、pass-through、非法 0/129、额度不足阻止上游提交、Ali 到 OpenAI 重试时数量/附加费不泄漏。
- 钱包补充保留与全额退款测试：通过。
- 权限 scoped-policy 回归测试：通过。

### 前端

- `bun run typecheck`：通过。
- `bun run test`：58 个测试文件、429 项测试全部通过。
- `bun run build`：通过。
- 本次变更文件的定向 `oxlint`：通过。

### 隔离数据库与候选镜像

- 执行过 `scripts/verify-upstream-test.sh verify`，并成功完成。
- 候选镜像：`new-api-upstream-test:f0bdcc4f0058-dirty.5dcba57a7fe4-candidate`。
- 候选 API 使用 PostgreSQL 启动并通过 healthcheck；镜像 revision 与候选工作树一致。
- PostgreSQL 与 MySQL 的隔离跨数据库额度/锁/结算测试通过。
- Docker 构建结束后已执行 `docker builder prune -af`；输出显示清理约 7.457 GB 构建缓存。

### 已知的仓库基线检查问题

以下全局检查失败，但均为本轮未修改文件已有的基线问题；本轮相关文件的定向检查已通过：

- `bun run lint`：大量既有 oxlint 错误。
- `bun run format:check`：既有格式问题；本轮新增 hook 测试和修改后的 drawer 已不在失败列表中。
- `bun run copyright:check`：既有 6 个文件头问题；本轮新增测试文件已不在失败列表中。

## 当前工作区（预期）

除 `backups/` 外，工作区中的修改都是本轮待提交改动。重点包括：

- `.dockerignore`
- `scripts/verify-upstream-test.sh`
- `common/model.go`、`common/model_test.go`
- `dto/openai_image.go`
- `pkg/billingexpr/*`
- `relay/channel/ali/*`、`relay/channel/openai/relay_image.go`
- `relay/common/relay_info.go`
- `relay/helper/*`
- `relay/image_handler.go`、`relay/image_billing_test.go`
- `service/authz/*`
- `service/image_billing.go`、`service/billing_session.go`、`service/funding_source.go`、`service/log_info_generate.go`、`service/tiered_settle*`
- `types/price_data.go`
- 上述 `web/default` 三个渠道表单相关文件。

在继续前先运行：

```bash
git status --short --branch
git diff --check
```

不要把 `backups/` 加入暂存区。

## 未完成步骤（建议严格按此顺序）

1. 阅读本文件和当前 `git diff`，确认改动范围仍与上述一致。
2. 可选但建议：针对已经构建好的候选镜像，再补做 MySQL 与 SQLite 的完整应用启动/AutoMigrate healthcheck。之前的完整候选 API 启动已覆盖 PostgreSQL；MySQL/PostgreSQL 的跨库 model 测试已通过。
3. 重新执行关键验证（至少 `go test ./...`、前端测试/构建，或在未变更代码时保留本文件中的结果）。
4. 创建新的本地提交，例如：

   ```bash
   git add .dockerignore common dto pkg relay scripts service types web/default/src/features/channels
   git commit -m "fix: harden image billing and authorization policies"
   ```

   只提交本轮代码与测试；不要 `git add backups/`。提交前再次审阅暂存区。
5. **部署前重新备份生产数据库**。使用已有的生产备份流程；不要将凭据输出到终端、日志或文档。完成后验证备份文件可读、大小合理，并保留旧备份。
6. 部署应复用现有生产部署脚本/流程，并确保有明确的回退目标：当前生产版本和本次新提交 SHA。
7. 部署后至少验证：容器 health/restart count、`/api/status`、生产日志中的 migration/error、数据库 schema 未出现非预期变更、核心管理员页面和一次无副作用的图像请求路径。
8. 不要在没有用户进一步授权的情况下 `git push` 到远程；当前用户的“推送到线上”上下文授权的是生产部署，但此前明确要求过不推送 Git。若部署脚本本身不需要 Git push，保持不推送。

## 数据库风险结论（截至交接时）

- **Schema/数据迁移风险：低。** 本轮没有修改 `model/` 的 GORM 数据模型、没有新增/删除列、没有数据回填/转换逻辑。候选 PostgreSQL 启动、SQLite 全量测试、PostgreSQL/MySQL 跨库测试均已通过。
- **运行时数据行为风险：中等。** Authz 会保守拒绝 legacy scoped policy；图片计费会对无效数量和可用额度不足返回更早、更严格的错误，并纠正预扣/退款数额。这些是刻意的安全行为变化，而不是结构迁移。
- **UI 风险：低。** 变更局限于已有多密钥渠道的策略选择与已授权更新负载；有前端单元测试、类型检查、全量测试与构建覆盖。

## 隔离环境说明

- 隔离容器仍在运行，均不接触生产数据：`new-api-upstream-test-api`、`new-api-upstream-test-postgres`、`new-api-upstream-test-mysql`、`new-api-upstream-test-redis`。
- 默认隔离端口：API `3302`、PostgreSQL `35432`、MySQL `33306`。
- 如需停止隔离环境但保留其测试卷：

  ```bash
  docker compose --project-name new-api-upstream-test --file docker-compose.upstream-test.yml down
  ```

  不要使用 `clean` 或 `down --volumes`，除非明确确认不再需要隔离测试数据。
